export interface LightsaberProps {
  position: [number, number, number];
  color: string;
  rotation?: [number, number, number];
}

export interface Vector3 {
  x: number;
  y: number;
  z: number;
}
